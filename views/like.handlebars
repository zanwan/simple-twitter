<div class="container">
  <div class="row">
    {{> blockUserProfile user}}


    {{!-- Like貼文牆時間軸 --}}
    <div class="col">

      {{!-- 原推文 --}}
      <div class="row my-4">
        <div class="col">
          <h3>Like</h3>
          {{#each tweets as |tweet|}}
          {{#> blockLikePost}}
          {{tweet.value}}
          {{/ blockLikePost}}
          {{/each}}
        </div>
      </div>
      {{!-- ------ --}}

    </div>
  </div>
</div>