<div class="container">
  <div class="row">
    {{!-- 推主個人檔案 --}}
    {{>blockUserProfile data = user}}


    {{!-- 個人貼文牆時間軸 --}}
    <div class="col">
      {{!-- 原推文 --}}
      <div class="row my-4">
        <div class="col">
          {{#each tweets as |tweet|}}
            {{#> blockTweetPost}}
              {{tweet.value}}
              {{/ blockTweetPost}}
          {{/each}}
        </div>
      </div>
      {{!-- ------ --}}

    </div>
  </div>
</div>