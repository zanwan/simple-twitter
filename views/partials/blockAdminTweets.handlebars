<tbody>
  <tr>
    <th scope="row">{{this.id}}</th>
    <td>
      <a href="/users/{{this.id}}/tweets"><img src={{this.avatar}} id="avatarProfile">{{this.name}}
      </a>
    </td>

    <td>{{description}}</td>
    <td>30</td>
    <td>40</td>
    <td>2020/03/10</td>
    <td>
      {{!-- <a href="#" type="button" class="btn btn-danger btn-sm">Delete</a> --}}
      {{!-- {{#if .../user.isAdmin}} --}}
      <form action="/admin/tweets/{{this.id}}?_method=DELETE" method="POST" style="float: right;">
        <button type="submit" class="btn btn-danger btn-sm">Delete</button>
      </form>
      {{!-- {{/if}} --}}
    </td>
  </tr>
</tbody>